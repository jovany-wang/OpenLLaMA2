1. on Ray 跑起来 最小实现集合
    4 models 部署在不同的物理位置.

  - holding models on different GPUs
  - 
2. 

exps = actor_group1.make_exp.remote(x) # actor model

  while True:
    actor_group2.f.remote(exps) # critical model

  actor_group3.x
  actor_group4.x
  remote_actor -> actor group


3. 手动注意各个传输的细节

4. Propose systemic methods
